@startuml
left to right direction
skinparam defaultFontName "Fira Code"
skinparam monochrome false
skinparam packageStyle rect
skinparam shadowing false

!define ORANGE #DE6733
!define DARK #282C34
!define LIGHT #F2EFEA
!define BROWN #523024

skinparam struct {
    BackgroundColor LIGHT
    ArrowColor BROWN
    BorderColor BROWN
    HeaderBackgroundColor ORANGE
    HeaderFontColor White
    FontColor BROWN
    AttributeFontName "Consolas"
    AttributeFontSize 11
}

skinparam enum {
    BackgroundColor LIGHT
    ArrowColor BROWN
    BorderColor BROWN
    HeaderBackgroundColor ORANGE
    HeaderFontColor White
    FontColor BROWN
    AttributeFontName "Consolas"
    AttributeFontSize 11
}

skinparam interface {
    BackgroundColor White
    BorderColor ORANGE
    FontColor BROWN
}

skinparam note {
    BackgroundColor #FEF9E7
    BorderColor ORANGE
}

skinparam package {
    BorderColor BROWN
    FontColor BROWN
}

title Архитектура engine

package "engine" {
    struct "EngineContext<Redraw, Start>" as EngineContext {
        - instance: Arc<Instance>
        - device: Arc<Device>
        - queue: Arc<Queue>
        - memory: EngineMemory
        - rcx: Option<RenderContext>
        - pipelines: PipelineCache
        - descriptors: DescriptorSetCache
        - sampler: Arc<Sampler>
        + physics_context: PhysicsContext
        + children: Children
        + assets: AssetsManager
        + frames: u64
        - redraw: Redraw
        - start: Start
        + new(event_loop: &EventLoop<()>, start: Start, redraw: Redraw) -> EngineContext
        + calculate_drawables(...) -> (Subbuffer<[MyVertex]>, Vec<Transform>, Vec<u32>)
    }

    struct RenderContext {
        - window: Arc<Window>
        - swapchain: Arc<Swapchain>
        - render_pass: Arc<RenderPass>
        - framebuffers: Vec<Arc<Framebuffer>>
        - viewport: Viewport
        - recreate_swapchain: bool
        - previous_frame_end: Option<Box<dyn GpuFuture>>
        - scale: Vec2
    }

    struct MyVertex {
        + position: [f32; 2]
    }
    
    interface ApplicationHandler << (T,ORANGE) >> {
        + resumed(&mut self, event_loop: &ActiveEventLoop)
        + window_event(&mut self, event_loop: &ActiveEventLoop, id: WindowId, event: WindowEvent)
        + about_to_wait(&mut self, event_loop: &ActiveEventLoop)
    }
}

package "Commands" {
    struct CommandQueue {
        - commands: Vec<DrawCommand>
        + append(&mut self, command: DrawCommand)
    }

    enum DrawCommand {
        DrawObject(Shapes, DrawableCreateInfo)
    }

    interface CommandDispatcher << (T,ORANGE) >> {
        + flush_commands(&mut self, commands: CommandQueue)
    }
}

package "Memory" {
    struct EngineMemory {
        + command_buffer_allocator: Arc<StandardCommandBufferAllocator>
        + memory_allocator: Arc<StandardMemoryAllocator>
        + descriptor_allocator: Arc<StandardDescriptorSetAllocator>
        + new(device: Arc<Device>) -> EngineMemory
    }
}

package "Resources" {
    struct AssetsManager {
        ~ queue: Arc<Queue>
        ~ memory_allocs: Arc<EngineMemory>
        + load(&self, file_name: &Path, internal: bool) -> TextureHandler
    }

    struct TextureHandler {
        ~ view: Arc<ImageView>
    }

    interface "CacheProvider<A>" as CacheProvider << (T,ORANGE) >> {
        + get(&self, key: &str) -> Option<A>
        + insert(&self, value: (String, A)) -> Option<A>
    }
    
    struct PipelineCache {
        - pipelines: RwLock<HashMap<String, Arc<GraphicsPipeline>>>
        + new(pipelines: HashMap<String, Arc<GraphicsPipeline>>) -> PipelineCache
    }

    struct DescriptorSetCache {
        - descriptors: RwLock<HashMap<String, Arc<DescriptorSet>>>
        + new(descriptors: HashMap<String, Arc<DescriptorSet>>) -> DescriptorSetCache
    }
}

package "Movement" {
    interface Position << (T,ORANGE) >> {
        + get_matrix_mut(&mut self) -> &mut [[f32; 4]; 4]
        + get_matrix(&self) -> &[[f32; 4]; 4]
    }
    
    interface Entity << (T,ORANGE) >> {
        + rigid_body<'a>(&self, ctx: &'a mut PhysicsContext) -> &'a mut RigidBody
        + rb_handle(&self) -> RigidBodyHandle
    }
    
    package "Physics" {
        struct PhysicsContext {
            + rigid_body_set: RigidBodySet
            + collider_set: ColliderSet
            + space: PhysicsSpace
            + new(...) -> PhysicsContext
            + step(&mut self)
            + create_phys_square(...) -> PhysicsDrawable
        }

        struct PhysicsSpace {
            - integration_parameters: IntegrationParameters
            - pipeline: PhysicsPipeline
            - island_manager: IslandManager
            - broad_phase: DefaultBroadPhase
            - narrow_phase: NarrowPhase
            - impulse_joint_set: ImpulseJointSet
            - multibody_joint_set: MultibodyJointSet
            - ccd_solver: CCDSolver
            - physics_hooks: ()
            - event_handler: ()
            + new() -> PhysicsSpace
        }

        interface DynamicObject << (T,ORANGE) >> {
            + teleport(&mut self, ctx: &mut PhysicsContext, vec: Vec2)
        }
    }
}

package "Drawing" {
    struct Children {
        + drawables: Vec<Drawable>
        + new() -> Children
        + add_drawable(&mut self, item: Drawable)
    }

    struct Drawable {
        - transform: Transform
        - color: Rgba8
        ~ render: DrawableRenderContext
        + new(...) -> Drawable
        + new_with_color(...) -> Drawable
        + from_shape(shape: Shapes, drw: DrawableCreateInfo, pipeline: Arc<dyn Pipeline>, mem_alloc: Arc<dyn MemoryAllocator>, desc_alloc: Arc<dyn DescriptorSetAllocator>, sampler: Option<Arc<Sampler>>) -> (Drawable, Option<Arc<DescriptorSet>>)
    }

    struct PhysicsDrawable {
        - rb_h: RigidBodyHandle
        - drawable: Drawable
        + new(rb_h: RigidBodyHandle, drawable: Drawable) -> PhysicsDrawable
        + get_rb(...) -> &mut RigidBody
        + get_rb_handle(&self) -> RigidBodyHandle
        + get_drawable(&self) -> &Drawable
        + get_mut_drawable(&mut self) -> &mut Drawable
    }

    struct DrawableRenderContext {
        ~ descriptor_id: DescriptorID
        - pipeline_id: PipelineID
        - mesh: Mesh
    }

    struct PipelineID {
        + id: String
    }

    struct DescriptorID {
        + id: String
    }

    struct Mesh {
        - vertex: Vec<MyVertex>
        - id: u32
        + new(...) -> Mesh
        + get_id(&self) -> &u32
    }
    
    struct DrawableCreateInfo {
        + position: Vec2
        + texture: Option<Texture>
        + radius: f32
        + thickness: f32
        + size: Vec2
        + id: u32
        + color: Rgba8
    }

    struct Texture {
        ~ image: Vec<u8>
        + dimensions: (u32, u32)
        + from_file(path: &str) -> Option<Texture>
        + from_internal_assets(filename: &str) -> Option<Texture>
    }

    interface DrawableGPU << (T,ORANGE) >> {
        + set_vertex(&mut self, vertex: Vec<MyVertex>)
        + get_vertex_clone(&self) -> Vec<MyVertex>
        + get_vertex(&self) -> &Vec<MyVertex>
        + get_colour(&self) -> &Rgba8
    }

    interface DrawableComponent << (T,ORANGE) >> {
        + get_transform(&self) -> &Transform
        + get_transform_clone(&self) -> Transform
        + set_transform(&mut self, transform: Transform)
        + drawable(&self) -> &Drawable
        + drawable_mut(&mut self) -> &mut Drawable
    }
}

package "Geometry" {
    struct Transform {
        + transform: [[f32; 4]; 4]
    }

    enum Shapes {
        Square
        Circle
        Image(TextureHandler)
        + get_vertex_and_descriptor(&self, pipeline: Arc<dyn Pipeline>, mem_alloc: Arc<dyn MemoryAllocator>, desc_alloc: Arc<dyn DescriptorSetAllocator>, sampler: Option<Arc<Sampler>>) -> (Vec<MyVertex>, Option<Arc<DescriptorSet>>)
    }
}

' Relationships
ApplicationHandler <|.. EngineContext : implement
CommandDispatcher <|.. EngineContext : implement

CacheProvider <|.. PipelineCache : implement
CacheProvider <|.. DescriptorSetCache : implement

EngineContext o-- RenderContext : contains (Option)
EngineContext *-- EngineMemory : own
EngineContext *-- PipelineCache : own
EngineContext *-- DescriptorSetCache : own
EngineContext *-- PhysicsContext : own
EngineContext *-- Children : own
EngineContext *-- AssetsManager : own
PhysicsContext *-- PhysicsSpace : own

CommandQueue o-- DrawCommand : contains

DrawableGPU <|-- DrawableComponent : extend
DrawableComponent <|.. Drawable : implement
DrawableGPU <|.. Drawable : implement
DrawableComponent <|.. PhysicsDrawable : implement
DrawableGPU <|.. PhysicsDrawable : implement

DynamicObject <|.. PhysicsDrawable : implement
Entity <|.. PhysicsDrawable : implement
Entity --|> DrawableComponent : extend
Entity --|> DynamicObject : extend

Position <|.. Transform : implement

Children o-- Drawable : contains

Drawable *-- Transform : own
Drawable *-- DrawableRenderContext : own
DrawableRenderContext *-- Mesh : own
DrawableRenderContext *-- PipelineID : own
DrawableRenderContext *-- DescriptorID : own
Mesh *-- MyVertex : own

PhysicsDrawable *-- Drawable : own
Shapes o-- TextureHandler : contains

note right of EngineContext
    Центральная точка входа.
    Управляет жизненным циклом
    приложения через ApplicationHandler и CommandDispatcher.
    Настраивается через замыкания
    (Start и Redraw).
end note

@enduml
